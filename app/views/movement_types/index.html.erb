<br><br><br>
<h1 class="text-center">Tipo de movimientos</h1>
<br><br><br>
<%= link_to 'Nuevo', new_movement_type_path, class: 'btn btn-primary offset-md-10 col-md-2' %>
<br><br>
<table id="dttb" class="table table-hover">
      <thead>
        <tr>
          <th>Nombre</th>
          <th>Tipo</th>
          <th>Activo</th>
          <th>Editar</th>
          <th>Eliminar</th>
        </tr>
      </thead>
      <tbody>
        <% @movement_types.each do |movement| %>
          <tr>
            <td><%= link_to movement.name.capitalize, edit_movement_type_path(movement) %></td>
            <td><%= movement.mov_type %></td>
            <td>
              <% if movement.active %>
                <%= fa_icon 'check-circle', class: "fa-2x text-success" %>
              <% else %>
                <%= '' %>
              <% end %>
            </td>
            <td><%= link_to 'Editar', edit_movement_type_path(movement), class: 'btn  btn-sm btn-outline-warning' %></td>
            <td><%= link_to 'Eliminar', movement_type_path(movement), class: 'btn btn-sm btn-outline-danger', method: :delete, data: { confirm: 'Estás seguro?' }  %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

<script>
  $(document).ready(function() {
    $("#dttb").dataTable({
        dom: 'Bfrtip',
        buttons: [
            'copy', 'csv', 'excel', 'pdf', 'print'
        ]
    });
  });
</script>